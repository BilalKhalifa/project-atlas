# Atlas shared project context

Before taking action, read `@/AGENTS.md` and `@/docs/PROJECT_CONTEXT.md`.

Those files are the shared project contract for Codex, Claude Desktop, and Antigravity. Follow the context-refresh workflow after every meaningful change. Do not treat older draft documentation or copied agent instructions as authoritative when they conflict with the canonical context.
